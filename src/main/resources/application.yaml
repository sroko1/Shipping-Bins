spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shipping_bins?serverTimezone=CET
    username: root
    password: java29
    drivateClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      dll-auto: verify
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  messages:
    basename: messages/messages
